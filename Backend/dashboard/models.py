# No new models; reusing existing models from other apps to avoid duplication
# Models used: TransactionLog, ReconciliationStats, InternetPlan, 
# Subscription, Router, RouterStats, HotspotUser, RouterHealthCheck